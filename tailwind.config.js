const { plugin } = require("daisyui/functions/plugin");

module.exports = {
    darkMode: ['selector', '[data-theme="dark"]'],
    purge: [],
    darkMode: false,
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugin: [],
}